/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'img.staging.kitabisa.cc',
      'kitabisa-userupload-01.s3-ap-southeast-1.amazonaws.com'
    ]
  },
};

export default nextConfig;
